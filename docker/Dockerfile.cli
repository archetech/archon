# Use the official Node.js as the base image
FROM node:22.23.2-trixie-slim AS builder

# Set the working directory
WORKDIR /app

# Copy package.json and package-lock.json to the working directory
COPY package*.json ./
COPY packages/ ./packages/

# Seed prebuild-install with natively-fetched binaries (scripts/prefetch-prebuilds.mjs).
# Empty in local builds, which falls back to prebuild-install's network fetch.
COPY prebuilds/ /prebuilds/
ENV npm_config_ipshipyard_node_datachannel_local_prebuilds=/prebuilds

# Install dependencies
RUN npm ci
RUN npm run build

COPY scripts ./scripts/

# Drop dev dependencies once everything is compiled.
RUN npm prune --omit=dev

# The build needs devDependencies and the runtime needs none of them. Layers are
# additive, so pruning in a later layer of one image reclaims nothing -- the
# install has to happen in a stage that is discarded (#987).
FROM node:22.23.2-trixie-slim

WORKDIR /app

# This image is exec'd into to run the CLI, so it carries the compiled
# workspaces, the pruned dependencies they resolve through, and scripts/.
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package*.json ./
COPY --from=builder /app/packages ./packages
COPY --from=builder /app/scripts ./scripts

# The CLI is the reason to exec into this image, so give it a name on PATH.
# scripts/archon-cli.js has no shebang and node_modules/.bin is not on PATH,
# so without this every invocation has to spell out the interpreter and path.
# It resolves its own package.json from __dirname, so any working directory
# works.
RUN printf '#!/bin/sh\nexec node /app/scripts/archon-cli.js "$@"\n' > /usr/local/bin/archon \
    && chmod +x /usr/local/bin/archon

ARG GIT_COMMIT=unknown
ENV GIT_COMMIT=$GIT_COMMIT

# Run idle
CMD ["tail", "-f", "/dev/null"]
